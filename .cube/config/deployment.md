# consilium — Deployment

## Environment
Local development only (MVP phase).

## Development
```bash
npm run dev    # Start dev server
npm run build  # Production build
npm run test   # Run tests
```

## Environment Variables
```
NEXT_PUBLIC_APP_VERSION=
LLM_API_KEY=         # Optional: default LLM provider key
LLM_BASE_URL=        # Optional: custom LLM base URL
LLM_MODEL=           # Optional: default model name
```

## Docker
TBD — not required for MVP phase.

## CI/CD
TBD — no CI configuration yet.
